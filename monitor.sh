#!/bin/bash
##########################################
source global.sh # подключение файла с переменными

##########################################
if [[ -z "$1" ]]; then
    echo sttt | iftop -n -N -i "$int"
else
    tail -c 20000 -f "$log_file";
fi

##########################################
